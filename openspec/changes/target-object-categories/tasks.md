## 1. Astro backend: generic category resolution

- [x] 1.1 In `astrobackend/catalog.py` replace `_resolve_double_stars(max_magnitude)` with `_resolve_category(otype, max_magnitude)`: same ADQL over `basic`/`allfluxes`/`otypes` with the requested code in place of the literal `'**'`, `TOP CANDIDATE_CAP + 1`, `lru_cache(maxsize=16)` keyed on `(otype, max_magnitude)`, and re-check the `^[A-Za-z0-9*?_+-]{1,8}$` shape inside the function so the module is safe independently of its caller; drop the `object_type="Double star"` override so `_fixed_candidate` types each row from its own `basic.otype`. Update `clear_cache()` to the new name. Verify with a `tests/test_search.py` test that monkeypatches `_simbad_query_tap`, asserts the emitted ADQL contains `otypes.otype = 'GlC'`, and that a canned row whose `otype` is `EB*` comes back typed "Eclipsing binary" rather than "Double star".
- [x] 1.2 Point `resolve_set` at `category` instead of `double_stars`, taking the code as a new argument, and make `TooManyCandidates` name the category via `otype_label(otype)` rather than saying "double stars"; verify with a test that a canned 2001-row table yields a 400 whose message contains the readable category name and asks for a lower magnitude limit.
- [x] 1.3 Extend `_OTYPE_LABELS` to cover every code the frontend picker will offer (task 3.1), so a picker label and a candidate's label always read the same; verify by extending the existing `test_otype_labels` with the new codes and asserting none of them falls through to the shape-based guess.
- [x] 1.4 In `astrobackend/schemas.py` replace `double_stars` with `category` in `SET_KINDS` and add `otype` to `TargetSetSchema` as a string with `validate.Regexp(r"^[A-Za-z0-9*?_+-]{1,8}$")`; verify with schema-level tests that a code containing a quote, a space or nine characters is a 400 and that `'**'`, `'V*'`, `'GlC'` and `'Sy1'` are accepted.
- [x] 1.5 In `astrobackend/server.py` replace the `double_stars`/magnitude special case in `/api/resolve-targets` with the `category` one (missing code or missing magnitude → 400 before any catalog call) and pass the code through to `catalog.resolve_set`; update the endpoint's docstring, which still describes the double-star set. Verify with tests for a globular-cluster resolution through the monkeypatched seam, a `category` request with no magnitude and one with no code (both 400 with the seam raising, proving no catalog call), and a well-formed unknown code returning 200 with zero candidates.
- [x] 1.6 Rename or retire the `double_stars` tests in `tests/test_search.py` so no test still asserts the withdrawn kind, and confirm `make -C astrobackend check` is green offline.

## 2. Go backend: the object-type column and validation

- [x] 2.1 In `backend/db.go` add `OType string \`json:"otype"\`` to `TargetSearch`, swap `double_stars` for `category` in `setKinds`, and extend `Validate`: `category` requires a maximum magnitude and a code matching `^[A-Za-z0-9*?_+-]{1,8}$` (compiled once at package level), every other kind must store an empty code. Verify with table-driven cases in `backend/search_test.go` for a valid category search, a missing magnitude, an empty code, a code with a quote, a nine-character code, and `double_stars` now being rejected as an unknown set.
- [x] 2.2 In `backend/api.go` add `OType` to `SearchInput` and to `searchInputColumns`, and clear it in the bind step for every kind but `category` (mirroring how `Names` is cleared outside `names`); verify with a create/update round-trip test that a `category` search returns its code from `GET /api/searches`, and that a `planets` search submitted with a code is stored with an empty one.
- [x] 2.3 Confirm AutoMigrate adds the column cleanly and the seeded data is untouched: run `cd backend && go test -v` (SQLite AutoMigrate) and, once the stack is up in task 5.2, check that the existing `Planets` search still lists with an empty `otype`.

## 3. Frontend: the category set and its picker

- [x] 3.1 Create `obs-ui/src/categories.js` exporting the curated list as `{group, code, label}` entries grouped as stars, double and multiple stars, variable stars, clusters, nebulae and interstellar matter, and galaxies and beyond, plus a `categoryLabel(code)` helper that falls back to the raw code; verify `npm run build` passes and that every code in the file is also in `_OTYPE_LABELS` (task 1.3) with the same wording.
- [x] 3.2 In `obs-ui/src/searches.jsx` replace the "Double stars" entry of `setKindOptions` with "Object category" (`category`), add a `required` rule to the `set_kind` form item so a search saved under the withdrawn kind cannot be re-saved without choosing one, and render an antd `Select` with `options` grouped from `categories.js`, `showSearch` and label filtering, shown only when the kind is `category` and required then; verify the form opens with the double-or-multiple-star category preselected for a new search, that typing "glob" narrows the list to globular clusters, and that the picker renders with the astro service stopped.
- [x] 3.3 Thread the code through `toDefinition` (emit `otype` only for `category`, empty otherwise), `toFormValues` (seed the picker from the saved `otype`), `setKey` (include the code, so switching category discards the candidates) and `setSummary` (render `categoryLabel(otype)` plus the magnitude limit, and fall back to the raw `set_kind` for a search stored under the withdrawn kind); verify that changing the category after evaluating clears both the candidate table and Save, that changing only the brightness keeps the candidates, and that a `double_stars` row still summarises without throwing.
- [x] 3.4 Send `otype` in the `/api/resolve-targets` body from `evaluate()` and in the Save body to the Go server; verify end to end against a running stack that a globular-cluster search at magnitude 9 evaluates, saves, and reloads with its category intact in the edit form.

## 4. Sky view marker shapes

- [x] 4.1 In `obs-ui/src/obs.jsx` widen `markerKind()`: add the star-like labels the broader vocabulary introduces (supergiant, subdwarf, carbon star, T Tauri and the rest of the picker's stellar labels) to the star branch, and route point-like non-stars (quasar, radio source, X-ray source) to the dot branch, leaving clusters, nebulae and galaxies on the extended default. Verify by selecting a saved globular-cluster search and a saved quasar search and confirming the markers differ and match the tooltip's type text.

## 5. Documentation and verification

- [x] 5.1 Update `CLAUDE.md` where the `set_kind` vocabulary appears (the database-model paragraph and the astro-endpoint paragraph) to name `category` and its object-type code, and note that the curated picker list lives in `obs-ui/src/categories.js` while the backends stay generic over codes; verify by re-reading both paragraphs for any remaining mention of `double_stars`.
- [x] 5.2 Bump `VERSION` to 0.13.0 and add the matching README Versions entry describing the category set and the withdrawal of `double_stars`; verify the entry matches the file and follows the format of the 0.12.x entries.
- [x] 5.3 Run `make check` (UI build, `go test -v`, `pytest`) and confirm all three suites are green, then `make runserver` and walk the flow once: create a category search, evaluate it against Helsinki, save it, select it, confirm the sky view draws its matches with type-appropriate markers, and confirm a search still stored as `double_stars` lists, selects and draws but reports the invalid set when saved.
