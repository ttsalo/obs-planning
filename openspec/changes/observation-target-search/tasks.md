## 1. Backend: data model, validation and seeding

- [x] 1.1 In `backend/db.go` extend `TargetSearch` with the definition columns (`SetKind`, `MaxMagnitude *float64`, `Names` as a JSON-text type with array `MarshalJSON`/`UnmarshalJSON`, `StartTime`, `EndTime`, `StartDate`, `EndDate`, `Visibility`, `MaxBrightness`, `EvaluatedAt *time.Time`, `EvaluatedPosition`), drop `SearchStr`, and add the partial unique index `idx_searches_user_name` on `(user_id, name)`; verify `cd backend && go test -v` still passes with SQLite AutoMigrate creating the columns and index.
- [x] 1.2 Change `TargetObject` to a has-many child of `TargetSearch` (`TargetSearchID uint` indexed, drop the `many2many` tags) and add `Magnitude *float64`, `ObjectType string` and `Matched bool`; verify `TestSearches` still lists the seeded objects once `InitTestData` is updated in 1.4.
- [x] 1.3 Add `func (s *TargetSearch) Validate() error` and `func (o *TargetObject) Validate() error` in `db.go` covering the enums, required magnitude for `double_stars`, non-empty trimmed names for `names`, `HH:MM` times, optional `YYYY-MM-DD` dates with end ≥ start and span ≤ 31 days, non-empty candidate name, and RA 0..360 / Dec -90..90 for fixed candidates; verify with table-driven `TestTargetSearchValidate` and `TestTargetObjectValidate` in `backend/server_test.go`.
- [x] 1.4 Update `InitTestData` to seed "Planets" as a `planets` set (`none`/`D`, 22:00-02:00, evaluated for Helsinki) with the eight bodies as has-many candidates flagged matched, and to backfill an existing "Planets" row whose `SetKind` is empty or that has no has-many candidates; verify with `TestInitTestDataBackfill` that seeds an old-style search into the SQLite fixture, runs `InitTestData`, and asserts one search with the definition and eight matched candidates.

## 2. Backend: searches CRUD endpoints

- [x] 2.1 In `backend/api.go` add `SearchInput` (definition fields + `candidates []TargetObjectInput`, each with `matched`), a `searchNameTaken` pre-check and an `ownSearch` ownership-scoped lookup mirroring `positionNameTaken`/`ownPosition`; verify existing tests still pass.
- [x] 2.2 Implement `POST /api/searches` (`h.createSearch`): bind, force `UserID`, validate definition and every candidate → 400, duplicate name → 409, create search and candidates in one transaction → 201 with the row and its `TargetObjects`; verify with `TestCreateSearch` (matched and unmatched candidates both come back with their flags), `TestCreateSearchInvalid` (one case per rule from 1.3) and `TestCreateSearchDuplicate`.
- [x] 2.3 Implement `PUT /api/searches/:id` (`h.updateSearch`): ownership lookup → 404, validate → 400, rename conflict → 409, then in one transaction update the definition/evaluation columns (explicit `Select` so zero values and nulls are written), delete the old `target_objects` rows for the search and insert the new ones → 200; verify with `TestUpdateSearchReplacesCandidates` (old rows gone, new ones listed with flags), `TestUpdateSearchReflags` (same candidates resubmitted with different flags are stored with the new flags), `TestUpdateSearchForeign` (404 as the second fixture user), `TestUpdateSearchRenameConflict` (409) and `TestUpdateSearchMissing` (404).
- [x] 2.4 Implement `DELETE /api/searches/:id` (`h.deleteSearch`): ownership lookup → 404, delete the search and its candidates → 204, no last-search rule; verify with `TestDeleteSearch` (candidate rows gone, GET no longer lists it, deleting the last one succeeds) and `TestDeleteSearchForeign`.
- [x] 2.5 Update `h.searches` to select the new columns and `Preload("TargetObjects")` through the has-many, register the three new routes in `RegisterDBEndpoints`, and update `TestSearches`; verify `cd backend && go test -v` is fully green and each new test cleans up the rows it created.

## 3. Astro backend: catalog resolution and search evaluation

- [x] 3.1 Add `astroquery` to `astrobackend/requirements.in`, run `make -C astrobackend update-reqs` to re-pin `requirements.txt`, and rebuild the venv; verify `pip install -r requirements.txt` succeeds locally and `python -c "import astroquery"` works, and that `python3 fetch_data.py` still runs.
- [x] 3.2 Create `astrobackend/catalog.py` with the `_simbad_query_objects(names)` and `_simbad_query_tap(adql)` seam (lazy `astroquery` import, `TIMEOUT` 30 s, `ROW_LIMIT` 2001, request/blank-result errors raised as `CatalogUnavailable`), an object-type code → label map, and `resolve_set(set)` returning `(candidates, unresolved)` for `planets` (no network), `messier` (fixed M1..M110 identifier list, magnitude filter applied on the table), `double_stars` (ADQL over `basic`/`allfluxes`/`otypes` with `TOP 2001`, raising `TooManyCandidates` past 2000) and `names` (built-in bodies split off case-insensitively, rest via SIMBAD, missing rows reported as unresolved), with `lru_cache` per kind+magnitude and per names tuple; verify with `tests/test_search.py` using monkeypatched seams returning canned `Table`s for each kind, the mixed-names case, the 2001-candidate rejection and the unavailable case.
- [x] 3.3 Add the request/response schemas in `schemas.py` for `/api/resolve-targets` (set with kind/magnitude/names; response candidates, count, unresolved) and `/api/filter-targets` (candidates ≤ 2000 with name/ss_obj/ra/dec, lat, lon, `obs_window`, `windows` list of ISO start/end with ≤ 31 entries, each ≤ 24 h and end after start, visibility and brightness enums; response one flag per candidate and the count); verify schema-level 400s with tests for 32 windows, an inverted window, 2001 candidates and a bad enum.
- [x] 3.4 Implement `POST /api/resolve-targets` in `server.py` calling `catalog.resolve_set`, mapping `TooManyCandidates` to 400 and `CatalogUnavailable` to 502 `{"error": "catalog", "message"}`; verify with tests for each kind through the monkeypatched seam, `planets` resolving while the seam raises, the 2001-candidate 400 and the 502.
- [x] 3.5 Implement `POST /api/filter-targets` in `server.py` without any import of `catalog.py`: short-circuit to all-true when there are no windows or `none`+`D`, otherwise build the concatenated 30-minute sample `Time` array (end instants included), compute fixed candidates with one broadcast `SkyCoord`→`AltAz` transform and solar-system candidates with `get_body`, the Sun upper-limb altitude series once, apply the visibility (strict inequalities as `checkObsWindow`) and brightness (−18/−12/−6/0) masks and flag candidates with any passing sample, returning flags in request order; verify with tests for the mid-window rise, midsummer lat 60 with `NT` (nothing matched), the three-night match, the `none`+`D` all-true shortcut, flag order with a mixed list, and filtering answering while the SIMBAD seam raises.
- [x] 3.6 Measure the filter's worst case (2000 fixed candidates × 31 full-day windows) with `time` in a test or script and record the wall time in the design's Open Questions; verify it completes under 45 s on the dev machine, or introduce the subsampling described there and document it.

## 4. Astro backend: batched positions and fixed-object paths

- [x] 4.1 Add `GetObjsPostSchema`/`GetObjsResultSchema` and implement `POST /api/get-objs` in `server.py`: fixed targets through one vectorized transform, solar-system targets through `_compute_body`/`_apparent_radius_deg`/`_moon_phase`, ≤ 2000 targets, order and names preserved; verify with tests for the mixed Mars/Moon/M31 batch (radius and phase presence per entry) and equality of Jupiter's alt/az/radius with `/api/get-obj`.
- [x] 4.2 Add optional `ra`/`dec` to `GetObjTSPostSchema` and make `get_obj_timeseries` compute a fixed `SkyCoord` series when both are present, `sun_alt` from `_sun_alt_series`; verify with a test that the M31 series has 48 samples and altitudes consistent with Dec 41.27 at lat 60, and that the existing planet timeseries tests are unchanged.
- [x] 4.3 Register the new schemas in the flasgger definitions, add a `network` marker deselected by default in `pytest.ini` with one live SIMBAD test for M31, and confirm `make -C astrobackend check` and the CI steps (`pip install`, `fetch_data.py`, `pytest`) pass offline; verify by running the check with networking disabled (for example `unshare -n` or by pointing the seam at an unreachable host).

## 5. Frontend: searches dialog

- [x] 5.1 Create `obs-ui/src/searches.jsx` exporting `SearchesDialog({open, onClose, session, setSession, shownDate})` with `useQuery(["searches"])`, a list showing name, set summary, "N of M match" and evaluation time, a Selected tag, and Select/Edit/Delete actions (delete via `Popconfirm`, no disabled-last rule); verify `npm run build` passes and the list renders the seeded "Planets" search as "8 of 8 match".
- [x] 5.2 Add selection via `updateSession` with the full session and the new `search` name, and the rename-moves-selection handling in the update mutation; verify selecting a search changes the header, survives a reload, and renaming the selected search keeps it selected.
- [x] 5.3 Build the definition form: name, `set_kind` radio group, conditional `max_magnitude` (required for `double_stars`), `names` text area, start/end `TimePicker`, optional `RangePicker` day range, `visibility` and `max_brightness` radio groups, defaults 22:00-02:00 / no range / `window` / `NT`, with form state `{candidates, matches}` seeded from the saved search when editing and an `onValuesChange` handler that clears `matches` on any change and also clears `candidates` when the set fields (kind, magnitude, names) differ from the ones the candidates were resolved for; verify the form opens for add and edit with the right initial values, that changing the brightness keeps the candidate table while changing the magnitude empties it, and the build passes.
- [x] 5.4 Implement Evaluate: derive the selected position from the shared `["positions"]` query; if `candidates` is null post the set to `${astroBase}/api/resolve-targets` and store the list, unresolved names and the set; then build the `windows` list from the day range or `shownDate` plus the wall-clock times (end rolled to the next day when not after start, serialised with `toISOString()`) and post the candidates, position, criteria and windows to `${astroBase}/api/filter-targets`, storing the flags; show every candidate in a `Table` (name, type, magnitude, matched tag, matched first) headed "N of M match", unresolved names in a warning `Alert`, and 400/502/timeout messages from either request in an error `Alert`; verify with the seeded Helsinki position that `planets` evaluates without network, a `names` set with "Vega, Notastar" lists Vega and flags Notastar, that re-opening a saved `messier` search and changing only the time window evaluates with the astro container's network disabled, and that a new `messier` set under the same conditions yields the catalog-unavailable message with the form intact.
- [x] 5.5 Implement Save (disabled until `matches` is set, disabled again on any definition change): create/update mutations posting definition + `evaluated_at` + `evaluated_position` + `candidates` (each with its `matched` flag) to the Go server, invalidating `["searches"]` on success and showing 400/409 messages in the form; verify a duplicate name keeps the form open with the message, and a successful save shows the new "N of M match" in the list.

## 6. Frontend: header and sky view integration

- [x] 6.1 In `obs-ui/src/App.jsx` add the "Search: <name>" header button (ellipsis-capped, "(no search)" when the session's search resolves to nothing or there are none) opening `SearchesDialog`, passing the currently shown date; verify both indicators fit in the header at a typical width and each opens its own dialog.
- [x] 6.2 In `obs-ui/src/obs.jsx` derive the selected search's matched candidates (unmatched ones never reach the canvas), add a batch `useQuery` on `/api/get-objs` for them and a `ResultMarkers` component rendering `ObsObject` for solar-system bodies and type-based markers (star, extended, other) for fixed objects, with the Sun still using `Target`; verify the "Planets" search looks as before, a saved Messier search with 40 of 110 matched draws exactly 40 markers, and the network tab shows one `get-objs` request per minute.
- [x] 6.3 Extend the hover tooltip to take alt/az from the batch entry and append type and magnitude lines for fixed objects, and make `useTargetPathData` post `ra`/`dec` for fixed objects; verify hovering a Messier marker shows name, type, magnitude and the next transition lines.
- [x] 6.4 Implement the path policy: every matched candidate gets `TargetPath` when there are ≤ 10 of them (module constant), otherwise only the hovered/tapped one; verify with a saved Messier search that only the hovered object's path appears and disappears on leave, and that the 8-planet search still draws every path.
- [x] 6.5 Add the missing-search fallback effect (select the first search, guarded like the position fallback) and the no-search rendering (grid plus Sun only); verify by deleting the selected search (view switches to the first remaining one) and by deleting all searches (only the Sun is drawn, header reads "(no search)").

## 7. Verification, docs and version

- [x] 7.1 Run `make check` and verify all three suites pass, then `make build` to confirm the astro image builds with the new dependency.
- [x] 7.2 Run `make runserver` against an existing local Postgres volume and verify startup logs show AutoMigrate adding the columns and index and `InitTestData` backfilling "Planets" without a duplicate, then walk through every scenario in both spec files as a manual checklist (the catalog scenarios need outbound network from the astro container).
- [x] 7.3 Bump `VERSION` to 0.12.0, add a matching `### 0.12.0` README Versions entry (searches dialog, resolve-then-filter evaluation with all candidates stored and flagged, the new Go and astro endpoints, the SIMBAD dependency and the browser-local time convention), and update CLAUDE.md's database model, frontend structure, two-server split and conventions sections for the new endpoints, `searches.jsx`, `catalog.py` and the outbound network requirement; verify the three files agree on the version and the described behavior.

## 8. Wrapping azimuth windows

- [x] 8.1 Drop the `MinAz > MaxAz` rule from `Position.Validate` in `backend/db.go` (keep the 0..360 range and the altitude ordering), flip the `inverted az window` case in `TestPositionValidate` to valid, and add `TestCreatePositionWrappingAzimuth` (125 → 45 stored and listed unchanged); verify `cd backend && go test ./...` passes.
- [x] 8.2 Add `azInWindow(pos, az)` in `obs-ui/src/transitions.jsx` (wrap when `min_az > max_az`, strict, equal limits empty) and use it from `checkObsWindow`; add an `extra` hint under the Max azimuth field in `positions.jsx`; verify `npm run build` passes and, in the browser, a 125 → 45 window saves and clips paths in the west while keeping them across north.
- [x] 8.3 Add `_az_in_window` in `astrobackend/server.py` and use it in the filter's `window` branch; add ranges and an altitude ordering check to `ObsWindowSchema`; verify with `test_filter_wrapping_azimuth_window` (Vega matched for 270 → 90, not for 90 → 270, not for equal limits) and new 400 cases in `test_filter_validation`.
- [x] 8.4 Record the rule in the specs (MODIFIED `observation-positions` requirements, amended `target-resolution` filtering), proposal, design (D10), README 0.12.0 entry and CLAUDE.md; verify `openspec validate observation-target-search --strict` passes.
